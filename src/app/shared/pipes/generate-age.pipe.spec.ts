import { GenerateAgePipe } from './generate-age.pipe';

describe('GenerateAgePipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateAgePipe();
    expect(pipe).toBeTruthy();
  });
});
