import { DepartamentPipe } from './departament.pipe';

describe('DepartamentPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartamentPipe();
    expect(pipe).toBeTruthy();
  });
});
